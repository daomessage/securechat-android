<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network & Storage -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Push Notifications (API 33+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 后台保活 (Q3 修复:Android 切到后台无法收消息/视频) -->
    <!-- ForegroundService:WS 长连接持锁,Doze/Cached 状态下不被 freeze -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- dataSync 类型:消息同步用 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- phoneCall 类型:来电时启动,系统优先级最高 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_PHONE_CALL" />
    <!-- WAKE_LOCK:Doze 下处理 FCM 唤醒时短暂持锁拉离线消息 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- 电池白名单引导:跳系统设置请求免电池优化 -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Media & Voice -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

    <!-- Camera (QR scan) -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:name=".SecureChatApp"
        android:label="@string/app_name"
        android:theme="@style/Theme.SecureChat"
        android:allowBackup="false"
        android:hardwareAccelerated="true"
        android:usesCleartextTraffic="false"
        tools:targetApi="33">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|keyboardHidden|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Deep Link：通知点击跳转到指定会话 -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:scheme="securechat" android:host="chat"/>
            </intent-filter>
            <!-- Deep Link：扫码加好友 -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="securechat" android:host="add"/>
            </intent-filter>
        </activity>

        <!-- FCM Service -->
        <service
            android:name=".push.FcmService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>

        <!-- 消息同步前台服务:登录后启动,持有 WS 长连接 -->
        <!-- foregroundServiceType=dataSync 是 Doze 模式下保持连接的关键 -->
        <service
            android:name=".service.MessagingForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync"/>

        <!-- 来电前台服务:FCM 唤起来电时启动,phoneCall 类型享受系统最高优先级 -->
        <service
            android:name=".service.CallForegroundService"
            android:exported="false"
            android:foregroundServiceType="phoneCall"/>

        <!-- FileProvider 用于"导出聊天" 分享 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"/>
        </provider>

    </application>
</manifest>
